; PlatformIO configuration for the ESP32 lamp node firmware.
; Kept minimal for Milestone 0 — no HTTP client / sensor libraries pinned
; yet, since no logic uses them yet. Those will be added in the milestone
; that implements telemetry submission.

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
