// Copy this file to secrets.h (gitignored -- see .gitignore) and fill in
// your real DEVICE_PASSWORD before opening this project in Wokwi. Never
// commit secrets.h itself; this .example file only ever holds placeholders.
//
// DEVICE_PASSWORD must match the DEVICE_SEED_PASSWORD you set on the
// backend (see backend/.env.example) -- that's what seeds the
// "lamp-device" operator account this firmware logs in as.

#pragma once

#define DEVICE_USERNAME "lamp-device"
#define DEVICE_PASSWORD "replace-with-your-real-DEVICE_SEED_PASSWORD"
